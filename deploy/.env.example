# Database
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASS=change_me_strong_password

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# MinIO
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=change_me_minio_password

# JWT
JWT_SECRET=change_me_at_least_32_chars_random_string

# Paths (server absolute path for prototype deploy output)
DEPLOY_ROOT=/opt/vibeproto/projects
STORAGE_ROOT=/opt/vibeproto/storage

# Preview URL (your server domain or IP)
BASE_PREVIEW_URL=http://YOUR_SERVER_IP:8090
