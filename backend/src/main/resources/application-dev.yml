spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/vibeproto?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai
    username: root
    password: root
  data:
    redis:
      host: 127.0.0.1
      port: 6379

minio:
  endpoint: http://127.0.0.1:9000

vibeproto:
  system:
    base-preview-url: http://127.0.0.1:8090

logging:
  level:
    com.vibeproto: debug
